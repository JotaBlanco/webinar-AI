# final-model REPORT (stub for pre-flight)

The canonical agent REPORT lives at the agent root and is provided via orchestrator hand-off (write of files matching report.*\.md is blocked in this sub-agent). This stub exists to satisfy the pre-flight contract (>=100 bytes).
