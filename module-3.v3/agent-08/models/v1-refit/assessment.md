# Assessment — v1-refit

## Verdict

**Shelved without running.** The m3.v2 cohort already converged on V1's
coefficients to 3 decimals (AGENTS.md). Refitting on the local data slice
would at best change yaw RMSE by ~1e-4 rad/s and CTE by <1 m — well inside
the noise band of V1's signed-bias residuals.

Time spent here is time not spent on a structurally novel candidate.
Registered as `refines-v1` / `shelved`.
