# v1-baseline

The m3.v2 ceiling model. Used here as the reference for every candidate.

- Formulation: kinematic single-track + understeer + first-order lag + per-segment δ₀
- See `code/v1_baseline.py`.
- Scored: yaw 0.00587, CTE 56.81 m (pooled).
