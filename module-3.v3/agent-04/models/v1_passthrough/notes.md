# v1_passthrough — V1 baseline as a registered candidate

## Formulation
`predict = predict_v1` — kinematic single-track + understeer + first-order lag
+ per-segment δ₀. Reference floor, NOT structurally-different.

`structure: same-as-V1`

## Why register it
Provides an explicit floor entry in the registry so comparisons against
candidates make the structural-novelty diff trivial to read.
