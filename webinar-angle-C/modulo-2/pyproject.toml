[project]
name = "webinar-angle-c-modulo-1"
version = "0.0.1"
description = "Bare harness — Tools + Context-seed only."
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "scipy",
    "pandas",
    "matplotlib",
    "cantools",
    "pycapnp",
    "zstandard",
]
