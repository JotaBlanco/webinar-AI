# Módulo 4 — + Modularity (full harness)

Components present:
- **Tools (1)**, **Memory/State (2)**, **Context-seed (3)**, **Planning (4)**, **Verification (5)**, **Modularity (6)**.

This is the full six-component harness. The challenge is the same as in modules 1-3; the only thing that changed is what you have available to solve it with. After this module, the BettaTech six-box diagram is fully populated, and the build/rent/hybrid table has a value in every cell.

Read `AGENTS.md` (which now references skills metadata-first), then `rpi/RPI_INSTRUCTIONS.md`, then the challenge. Outputs in `out/`. Skills under `skills/` are loaded on demand.
