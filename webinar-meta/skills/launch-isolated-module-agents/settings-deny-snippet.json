{
  "_comment": "Paste the contents of `permissions.deny` and `hooks` into <angle-root>/.claude/settings.json. Replace ABSOLUTE PATH PLACEHOLDERS with your actual paths. This snippet enforces method #2 (declarative deny) and is the prerequisite for method #3 (hook).",

  "_what_this_protects": "Hard-blocks cross-KB and cross-angle reads. Does NOT enforce intra-angle isolation (M1 reading M2) — that stays soft + verified. See SKILL.md 'per-agent granularity limit'.",

  "_replace_placeholders": {
    "<REPO_ROOT>": "Absolute path to webinar-AI repo root (e.g. /Users/javiquix/Desktop/quixdev/webinar-AI)",
    "<KB_PARENT>": "Absolute path to the parent dir of sibling KBs (e.g. /Users/javiquix/Desktop/quixdev/F1)",
    "<ANGLE_NAME>": "The current angle folder name (e.g. webinar-angle-C)",
    "<SKILL_PATH>": "Absolute path to launch-isolated-module-agents/hook-blocker.py"
  },

  "permissions": {
    "deny": [
      "Read(<KB_PARENT>/KB001/**)",
      "Read(<KB_PARENT>/KB002/**)",
      "Read(<KB_PARENT>/KB003/**)",
      "Read(<REPO_ROOT>/webinar-angle-A/**)",
      "Read(<REPO_ROOT>/webinar-angle-B/**)",
      "Read(<REPO_ROOT>/webinar-meta/**)",
      "Read(<REPO_ROOT>/<ANGLE_NAME>/_shared/**)",
      "Read(<REPO_ROOT>/<ANGLE_NAME>/_launch/**)",
      "Read(<REPO_ROOT>/<ANGLE_NAME>/process-log.md)",
      "Read(<REPO_ROOT>/<ANGLE_NAME>/RUN-LOG.md)",
      "Read(<REPO_ROOT>/<ANGLE_NAME>/_observations/**)",
      "Read(<REPO_ROOT>/car-sim-real-CLAUDE.md)",

      "Bash(cat:<KB_PARENT>/**)",
      "Bash(head:<KB_PARENT>/**)",
      "Bash(tail:<KB_PARENT>/**)",
      "Bash(less:<KB_PARENT>/**)",
      "Bash(grep:<KB_PARENT>/**)",
      "Bash(find:<KB_PARENT>/**)",
      "Bash(cat:<REPO_ROOT>/webinar-angle-A/**)",
      "Bash(cat:<REPO_ROOT>/webinar-angle-B/**)",
      "Bash(cat:<REPO_ROOT>/webinar-meta/**)"
    ]
  },

  "_note_on_intra_angle": "We deliberately do NOT deny <REPO_ROOT>/<ANGLE_NAME>/modulo-* — each agent is supposed to read its own module. Per-agent path differentiation cannot be expressed in settings.json; the hook (method #3) cannot reliably do it either. Intra-angle isolation is prompt-soft + post-run-verified, by design and by limitation.",

  "_note_on_pattern_syntax": "Claude Code accepts `Read(/abs/path/**)` (glob) and `Bash(cmd:/abs/path/**)` (command+pattern). Patterns with spaces or special chars must be in valid glob form. Test by trying to read a denied path from inside the session and confirming the tool call is rejected before subagents launch.",

  "_note_on_KB003_symlink_collision": "<REPO_ROOT>/data/{raw,sim-only,sim-full} are now symlinks INTO KB_PARENT/KB003. The Read(<KB_PARENT>/KB003/**) deny rule above is intended to catch DIRECT absolute-path reads of KB003, NOT to block legitimate reads agents make through their data/ symlink. Behaviour depends on whether Claude Code's permissions.deny matches the literal path argument (safe) or the canonicalised path after symlink resolution (would block legitimate data/ reads). SMOKE TEST before relying on this: from inside a sub-agent session, try `Read(<REPO_ROOT>/data/sim-only/<a-real-platform>/...)` and confirm it succeeds. If it fails, remove the Read(<KB_PARENT>/KB003/**) line — the hook (layer 3) is still the catch-all for direct KB003 reads via absolute path."
}
